// #include <json.h>
#include <regex>
#include "vkapi.h"
void test(vkapi * vkapiObject) {
    char * lastResponse = vkapiObject->lastresponse;
    int i = 0;
    std::srting s_response("{\"response\":");
    std::regex r_response(s_response);
    while
}